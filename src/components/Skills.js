export default function Skills() {
  // - skills (frontend, backend, LLM prompting & model black magic fuckery, custom ML model) -
  //   languages (include Indonesian?)
  // frameworks - docker, git, aws, postgresql

  return (
    <section
      id="skills"
      className="min-h-screen bg-gray-300 flex justify-center items-center"
    >
      <h2 className="text-3xl font-bold">My Skills</h2>
    </section>
  );
}
